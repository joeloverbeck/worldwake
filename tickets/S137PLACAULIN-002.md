# S137PLACAULIN-002: CognitiveProfile repair-budget fields

**Status**: PENDING
**Priority**: HIGH
**Effort**: Medium
**Engine Changes**: Yes — `CognitiveProfile` profile component on `EntityKind::Agent`
**Deps**: specs/S137-plan-causal-links-and-repair.md (D11)

## Problem

S137 deliverable D11 adds two per-agent budget fields to `CognitiveProfile`:
- `repair_budget_fraction: Permille` — bounds the repair search at `repair_budget_fraction × max_node_expansions`.
- `causal_links_per_step_cap: u8` — caps `PlanGuard.causal_links` length (consumed by ticket 004's field addition).

Without these fields, downstream tickets (004 reads the cap; 006 reads the budget fraction) cannot enforce the bounded-repair invariant from S137 Design Goal 3.

## Assumption Reassessment (2026-05-13)

<!-- Apply all domain-specific precision rules from docs/precision-rules.md -->

1. `CognitiveProfile` is defined at `crates/worldwake-core/src/cognitive_profile.rs:23-114` (32+ fields) with `Default` impl at lines 117-155. No existing `repair_budget_fraction` or `causal_links_per_step_cap` field — confirmed by grep. Component registered at `crates/worldwake-core/src/component_schema.rs:1009-1027`. `AgentDef.cognitive_profile: Option<CognitiveProfile>` at `crates/worldwake-cli/src/scenario/types.rs:593`.
2. Spec `specs/S137-plan-causal-links-and-repair.md` D11 specifies field types, `#[serde(default)]`, and default values (`Permille::new_unchecked(250)` and `8`). `Permille` is defined at `crates/worldwake-core/src/numerics.rs:24-65` with `new_unchecked(value: u16)` (compile-time const, lines 31-47).
3. Shared boundary: the universal profile-component contract. `CognitiveProfile` is universal-on-Agent per `docs/spec-drafting-rules.md` Section 5 — `unwrap_or_default()` applied in `spawn_agent()`, `Default` impl mandatory.
4. Existing focused tests covering `CognitiveProfile` construction live in `crates/worldwake-core/src/cognitive_profile.rs:239+` (`#[cfg(test)]` block). New fields must roundtrip through bincode and tolerate pre-bump byte streams via `#[serde(default)]`.
5. **Construction-site spread-syntax audit**: 16 construction sites located by `rg '^\s*CognitiveProfile\s*\{$' crates/`. Spot-check shows mixed pattern:
   - `crates/worldwake-systems/src/evidence_decay.rs:188` uses `..CognitiveProfile::default()` spread — auto-picks up new fields, no edit needed.
   - `crates/worldwake-ai/src/agent_tick/planning.rs:2658`, `crates/worldwake-ai/src/decision_runtime.rs:418`, and most test fixtures explicitly enumerate fields — explicit additions required.
   Effort tracks the explicit-enumeration count (~14-15 sites) rather than the raw site total, but new fields have meaningful `Default` values + `#[serde(default)]`, so save-load and scenario RON tolerate pre-bump streams.
6. Generated documentation: `docs/profiles/all-profiles.md` is regenerated by `scripts/profile_docs.py`; the script reads `CognitiveProfile` field reflection. Field additions require re-running the script and committing the generated doc.

## Architecture Check

1. **Per-FND-22 per-agent variation**: budget fraction is per-agent rather than a global constant. Agents in different cognitive states (panicked, rested, wounded) can vary repair behavior. Aligns with the existing pattern of `max_node_expansions`, `decision_history_alternatives`, etc.
2. **No back-compat shim**: `#[serde(default)]` on the new fields handles save-load and scenario-RON forward-compatibility without an explicit migration path. Pre-S137 saves deserialize with default values rather than failing.

## Verification Layers

1. Field-presence + default value → focused unit test in `cognitive_profile.rs` `#[cfg(test)]`.
2. Save-load tolerance → bincode roundtrip test asserts pre-S137 byte streams (recorded fixture) deserialize with default field values.
3. Scenario RON tolerance → existing `golden_*` tests that load scenario files act as integration coverage; if they pass without scenario updates, RON-level `#[serde(default)]` works.
4. Single-layer in production code (profile-field addition); additional layer mapping not applicable per precision-rules.md item 6.

## What to Change

### 1. Add fields to `CognitiveProfile`

In `crates/worldwake-core/src/cognitive_profile.rs:23-114`, append:

```rust
#[serde(default = "default_repair_budget_fraction")]
pub repair_budget_fraction: Permille,
#[serde(default = "default_causal_links_per_step_cap")]
pub causal_links_per_step_cap: u8,
```

Place fields together at the end of the struct, after `slot_weights`. Add the two `default_*` helper functions at module scope below the struct.

### 2. Update `Default` impl at lines 117-155

Add `repair_budget_fraction: Permille::new_unchecked(250)` and `causal_links_per_step_cap: 8` to the literal.

### 3. Update explicit-enumeration construction sites

At each site listed in Files to Touch, add the two new fields. For sites using `..CognitiveProfile::default()` (evidence_decay.rs:188), no edit is required — verify at PR review.

### 4. Regenerate generated documentation

Run `python3 scripts/profile_docs.py` and commit the updated `docs/profiles/all-profiles.md`. Verify diff shows only the two new fields with their defaults.

## Files to Touch

- `crates/worldwake-core/src/cognitive_profile.rs` (modify — struct + Default impl + helper fns + tests)
- `crates/worldwake-ai/src/failure_handling.rs` (modify — construction site at 1921)
- `crates/worldwake-ai/src/decision_runtime.rs` (modify — construction site at 418)
- `crates/worldwake-ai/src/plan_revalidation.rs` (modify — construction site at 1918)
- `crates/worldwake-ai/src/agent_tick/planning.rs` (modify — construction site at 2658)
- `crates/worldwake-ai/src/agent_tick/tests.rs` (modify — construction site at 169)
- `crates/worldwake-ai/src/goal_model.rs` (modify — construction site at 2419)
- `crates/worldwake-ai/src/search/tests.rs` (modify — construction site at 52)
- `crates/worldwake-ai/tests/conformance_execution_budget.rs` (modify — 3 sites at 21, 29, 36)
- `crates/worldwake-ai/tests/golden_exploration.rs` (modify — 2 sites at 1599, 2030)
- `crates/worldwake-ai/tests/golden_quantity_aware_acquisition.rs` (modify — site at 135)
- `crates/worldwake-ai/tests/golden_ai_decisions.rs` (modify — site at 1255)
- `crates/worldwake-systems/src/perception.rs` (modify — construction site at 1683; verify spread-vs-explicit during reassessment)
- `docs/profiles/all-profiles.md` (regenerate via `python3 scripts/profile_docs.py`)

## Out of Scope

- `PlanGuard.causal_links` field that consumes `causal_links_per_step_cap` — ticket 004.
- `plan_repair` module that consumes `repair_budget_fraction` — ticket 006.
- `SAVE_FORMAT_VERSION` bump — neither field requires a bump because both have `#[serde(default)]`. Sites with explicit construction supply the new fields at compile time.

## Acceptance Criteria

### Tests That Must Pass

1. `cargo test -p worldwake-core cognitive_profile` — existing + new tests pass.
2. `cargo test --workspace` — all sites construct `CognitiveProfile` cleanly; pre-existing tests unmodified semantically.
3. `python3 scripts/profile_docs.py --check` — generated docs match committed state (or whatever check flag the script supports; see `scripts/profile_docs.py` for the canonical invocation).
4. Existing suite: `cargo clippy --workspace --all-targets -- -D warnings`.

### Invariants

1. `Permille::new_unchecked(250)` is a compile-time constant (250 ≤ 1000); no runtime panic possible.
2. `CognitiveProfile::default()` produces `repair_budget_fraction == Permille::new_unchecked(250)` and `causal_links_per_step_cap == 8`.
3. Pre-S137 bincode byte streams deserialize successfully via `#[serde(default)]`.

## Test Plan

### New/Modified Tests

1. `crates/worldwake-core/src/cognitive_profile.rs` `#[cfg(test)]` — add `default_includes_repair_budget_fields` asserting default values; add `repair_budget_fraction_roundtrips_through_bincode`.
2. Optional: `cognitive_profile_deserializes_legacy_format` with a pre-bump byte fixture — proves `#[serde(default)]` works.

### Commands

1. `cargo test -p worldwake-core cognitive_profile`
2. `cargo test --workspace`
3. `cargo clippy --workspace --all-targets -- -D warnings`
4. `python3 scripts/profile_docs.py` followed by `git diff docs/profiles/all-profiles.md` to confirm only the two new fields surface in the diff.
