# S46GOLGAP-003: Regenerate golden coverage documentation for Scenario 57

**Status**: PENDING
**Priority**: MEDIUM
**Effort**: Small
**Engine Changes**: None — documentation-only ticket
**Deps**: S46GOLGAP-001, S46GOLGAP-002 (tests must exist and pass first)

## Problem

After adding Scenario 57 (patrol-driven crime discovery) in S46GOLGAP-001/002, the generated golden coverage documentation must be regenerated so that Scenario 57 appears in all three canonical inventories. Without regeneration, the docs will be stale and `--check-docs` will flag the discrepancy.

## Assumption Reassessment (2026-03-30)

1. **Script exists**: `scripts/golden_inventory.py` (540 lines) generates three files: `docs/generated/golden-e2e-inventory.md`, `docs/generated/golden-scenario-map.md`, `docs/generated/golden-coverage-matrix.md`. Confirmed at `/home/joeloverbeck/projects/worldwake/scripts/golden_inventory.py`.
2. **Script reads scenario annotations**: The script parses structured comment blocks in golden test files (the `// Systems:`, `// GoalKinds:`, etc. header format). Scenario 57's comment block must follow this format (established in S46GOLGAP-001).
3. **Generated docs directory**: `docs/generated/` exists and contains the three target files.
4. **No code changes needed**: This ticket only runs the script and verifies its output. The test code already includes the correct scenario annotations from S46GOLGAP-001.
5. **tickets/README.md reference**: "Regenerate both with `python3 scripts/golden_inventory.py --write --check-docs`" — this is the canonical command.

## Architecture Check

1. This follows the established post-implementation documentation workflow. No alternatives exist — the script is the canonical tool for coverage doc generation.
2. No backwards-compatibility shims.

## Verification Layers

1. Scenario 57 appears in generated inventory → `docs/generated/golden-e2e-inventory.md` file content
2. Scenario 57 metadata is correct → `docs/generated/golden-scenario-map.md` file content
3. Coverage matrix reflects new GoalKind/System coverage → `docs/generated/golden-coverage-matrix.md` file content
4. No duplicate scenario IDs → script `--check-docs` exit code
5. Single-layer ticket: only documentation generation. No runtime, trace, or authoritative layer is involved.

## What to Change

### 1. Run the golden inventory script

```bash
python3 scripts/golden_inventory.py --write --check-docs
```

This will:
- Parse all golden test files including the updated `golden_patrol.rs`
- Extract Scenario 57 metadata from the structured comment block
- Regenerate all three documentation files
- Validate no duplicate IDs or missing annotations

### 2. Verify output

After running the script, verify that:
- `docs/generated/golden-e2e-inventory.md` includes `golden_patrol_driven_crime_discovery` and its replay companion
- `docs/generated/golden-scenario-map.md` includes Scenario 57 with correct systems, GoalKinds, ActionDomains, Places, and Principles
- `docs/generated/golden-coverage-matrix.md` reflects the new coverage additions

## Files to Touch

- `docs/generated/golden-e2e-inventory.md` (modify — regenerated by script)
- `docs/generated/golden-scenario-map.md` (modify — regenerated by script)
- `docs/generated/golden-coverage-matrix.md` (modify — regenerated by script)

## Out of Scope

- Any engine/production code changes.
- Any test code changes.
- Manual editing of generated documentation files — the script is the sole author.
- Updating non-generated documentation (README, CLAUDE.md, etc.).

## Acceptance Criteria

### Tests That Must Pass

1. `python3 scripts/golden_inventory.py --write --check-docs` — exits cleanly with no errors.
2. `cargo test -p worldwake-ai --test golden_patrol` — all patrol golden tests still pass (no regressions from doc changes).

### Invariants

1. Scenario 57 appears in all three generated files with correct metadata.
2. No duplicate scenario IDs across all golden test files.
3. All previously documented scenarios remain present and unchanged.
4. Generated files are the sole output of the script — no manual edits mixed in.

## Test Plan

### New/Modified Tests

1. None — documentation-only ticket; verification is command-based and existing runtime coverage is named in Assumption Reassessment.

### Commands

1. `python3 scripts/golden_inventory.py --write --check-docs`
2. `cargo test -p worldwake-ai --test golden_patrol`
