# S147HTNMETDEC-003: AgentSchemaContextProfile.disabled_methods and S111 lint extension

**Status**: PENDING
**Priority**: MEDIUM
**Effort**: Small
**Engine Changes**: Yes — extends the universal `AgentSchemaContextProfile` component with a new `disabled_methods` denylist field and updates the `ProfileHomogeneity` scenario lint.
**Deps**: 001 (MethodSchemaId)

## Problem

S147's per-agent method enablement is gated by the `disabled_methods` field on `AgentSchemaContextProfile`. The denylist convention (empty default = all enabled, scenarios opt agents out) was chosen during reassessment over the original allowlist proposal to (a) match the sibling `disabled_extractors` convention, (b) preserve Design Goal #5 (peasants don't know `FulfillBountyGroupHunt` by default at the registry level, but scenarios opt them out explicitly via the lint-checked denylist), and (c) satisfy FND-22 (diversity through concrete per-agent variation). Without this field, the `MethodSelector` (ticket 007) has no per-agent enablement surface and method gating must live in code rather than scenario data.

## Assumption Reassessment (2026-05-17)

<!-- Apply all domain-specific precision rules from docs/precision-rules.md -->

1. `AgentSchemaContextProfile` lives at `crates/worldwake-core/src/agent_schema_context_profile.rs:54`. Current fields: `disabled_extractors: BTreeSet<CandidateExtractorId>` (line 56), `budget_overrides: BTreeMap<GoalDispatchKey, GoalPlanningBudget>` (line 57). Registered as ECS component at line 60. Default impl tests assert both existing fields empty at lines 70-71. Workspace construction sites: 1 production site at `crates/worldwake-ai/src/candidate_generation.rs:14017`. The component is universal per archived S146 and surfaces in scenario RON via `crates/worldwake-cli/src/scenario/types.rs:595`.
2. `ProfileHomogeneity` lint lives at `crates/worldwake-cli/src/scenario/lints.rs:28` (enum variant) with `check_profile_homogeneity` function at line 62. The lint already covers `disabled_extractors` and `budget_overrides` uniformity; extending it for `disabled_methods` follows the existing pattern.
3. `docs/profiles/all-profiles.md:76` documents `AgentSchemaContextProfile`. The doc is generated by `scripts/profile_docs.py` and the script picks up new fields from the struct definition automatically; re-running the script after adding the field is required.
4. `MethodSchemaId` lands in ticket 001 at `crates/worldwake-core/src/method_schema_id.rs`. With `#[serde(default)]` on the new field, no scenario RON file needs modification — existing scenarios deserialize cleanly with `disabled_methods` defaulting to empty.
5. Shared boundary: `AgentSchemaContextProfile` is the scenario-authored per-agent configuration surface. This ticket extends it with one denylist; ticket 007 (`MethodSelector`) is the consumer.

## Architecture Check

1. Denylist semantics (empty default = all enabled) match `disabled_extractors`. The original allowlist proposal (`enabled_methods` with default "all enabled") contradicted Design Goal #5 because a default of "all enabled" makes role-specific methods like `FulfillBountyGroupHunt` default-available to every agent, defeating diversity. The denylist puts the burden on scenario authors to opt agents out of methods, which is the right ergonomic direction for FND-22 (concrete per-agent variation).
2. `#[serde(default)]` on the new field preserves existing scenario RON deserialization without modification — no backwards-compatibility shim is needed and no SAVE_FORMAT_VERSION bump is required (sibling ticket 002 owns the only bump in S147).
3. Profile-doc regeneration (`scripts/profile_docs.py`) is in-scope to keep `docs/profiles/all-profiles.md` consistent with the code surface; deferring the doc regen would leave a doc/code mismatch invisible to Rust grep.

## Verification Layers

1. Field exists with correct shape and default → focused unit test in `agent_schema_context_profile.rs` tests asserting `Default::default().disabled_methods.is_empty()`.
2. Scenario RON without `disabled_methods` still deserializes cleanly → existing scenario-load tests pass (cross-crate verification that `#[serde(default)]` is honored at the scenario boundary).
3. `ProfileHomogeneity` lint flags uniform `disabled_methods` → new focused test in `lints.rs` constructs a scenario where every agent has identical `disabled_methods` and asserts the lint fires.
4. Single-layer ticket — this field is scenario configuration only; the consumer surface (`MethodSelector` in ticket 007) verifies the read path separately.

## What to Change

### 1. Add `disabled_methods` field to `AgentSchemaContextProfile`

Modify `crates/worldwake-core/src/agent_schema_context_profile.rs`:

```rust
pub struct AgentSchemaContextProfile {
    pub disabled_extractors: BTreeSet<CandidateExtractorId>,
    pub budget_overrides: BTreeMap<GoalDispatchKey, GoalPlanningBudget>,
    #[serde(default)]
    pub disabled_methods: BTreeSet<MethodSchemaId>,    // NEW
}
```

Extend the existing `Default::default()` test (around lines 70-71) to assert `disabled_methods.is_empty()`.

### 2. Update the single production construction site

Modify `crates/worldwake-ai/src/candidate_generation.rs:14017` to either (a) explicitly enumerate `disabled_methods: BTreeSet::new()` or (b) switch to `..Default::default()` spread syntax. Pick (a) if the surrounding test/fixture context enumerates fields explicitly; pick (b) if the site already uses spread.

### 3. Extend `ProfileHomogeneity` lint

Modify `crates/worldwake-cli/src/scenario/lints.rs::check_profile_homogeneity` (line 62) to add a `disabled_methods` uniformity check parallel to the existing `disabled_extractors` check. Emit `LintRule::ProfileHomogeneity` with a message naming the field when every agent's `disabled_methods` set is identical (including all-empty — that signals the scenario is not exercising per-role method enablement).

### 4. Regenerate profile documentation

Run `python3 scripts/profile_docs.py` after the field is added. The generated update lands in `docs/profiles/all-profiles.md`. Include the regenerated file in the commit.

## Files to Touch

- `crates/worldwake-core/src/agent_schema_context_profile.rs` (modify — add field + serde-default + extend Default test)
- `crates/worldwake-ai/src/candidate_generation.rs` (modify — update single construction site at line 14017)
- `crates/worldwake-cli/src/scenario/lints.rs` (modify — extend `check_profile_homogeneity` for `disabled_methods`)
- `docs/profiles/all-profiles.md` (modify — regenerated by script)
- `scripts/profile_docs.py` (run, no code change)

## Out of Scope

- `MethodSelector` consumption of `disabled_methods` (ticket 007).
- The `disabled_methods` field's interaction with `MethodRegistry` (ticket 006 — registry construction is independent of profile reading).
- Per-role default denylists (scenario authoring concern, not a code change in this ticket).

## Acceptance Criteria

### Tests That Must Pass

1. `AgentSchemaContextProfile::default().disabled_methods.is_empty()` asserted in existing inline test.
2. Existing `disabled_extractors`/`budget_overrides` Default assertions continue to pass (no regression).
3. Scenario RON deserialization tests pass without changes to scenario files (`#[serde(default)]` honored).
4. New `lints.rs` test: a scenario with two agents both having `disabled_methods = { MethodSchemaId(3) }` emits `LintRule::ProfileHomogeneity`.
5. `docs/profiles/all-profiles.md` regenerated and committed.
6. Existing suite: `cargo test -p worldwake-core`, `cargo test -p worldwake-ai`, `cargo test -p worldwake-cli` pass.

### Invariants

1. Existing scenario RON files continue to deserialize without modification (`#[serde(default)]` invariant).
2. No `SAVE_FORMAT_VERSION` bump needed — additive serde-default fields preserve backwards compatibility.
3. `ProfileHomogeneity` lint covers all three `AgentSchemaContextProfile` fields uniformly.

## Test Plan

### New/Modified Tests

1. `crates/worldwake-core/src/agent_schema_context_profile.rs` inline tests — extend Default test to cover `disabled_methods`.
2. `crates/worldwake-cli/src/scenario/lints.rs` inline tests — new case covering `disabled_methods` uniformity.

### Commands

1. `cargo test -p worldwake-core --lib agent_schema_context_profile`
2. `cargo test -p worldwake-cli --lib lints`
3. `python3 scripts/profile_docs.py` then `git diff docs/profiles/all-profiles.md`
4. `./scripts/verify.sh`
