# S27-007: Workspace verification and documentation

**Status**: PENDING
**Priority**: LOW
**Effort**: Small
**Engine Changes**: None
**Deps**: S27-001 through S27-006 (all implementation and golden test tickets must be complete)

## Problem

After all S27 implementation and golden test tickets are complete, the workspace needs a final verification pass and documentation updates to record the new violation-investigation and violation-report coverage entries.

## Assumption Reassessment (2026-03-25)

1. `docs/golden-e2e-coverage.md` is the coverage dashboard for golden test scenarios. It lists scenario descriptions, coverage domains, and which golden tests cover them. Confirmed by file read.
2. `docs/golden-e2e-scenarios.md` — need to verify if this file exists or if the scenario descriptions are in a different location. The spec references it but it may be named differently (e.g., `docs/generated/golden-scenario-map.md`, `docs/generated/golden-e2e-inventory.md`). Will check at implementation time.
3. `specs/IMPLEMENTATION-ORDER.md` should be updated to mark S27 as COMPLETED once all tickets are done. Confirmed pattern from other completed specs.
4. This is a documentation-only ticket. No code changes beyond docs.
5. `cargo test --workspace` and `cargo clippy --workspace` are the verification boundary. No narrower command is appropriate for final verification.

## Architecture Check

1. Documentation-only ticket; no architectural decisions.
2. No backwards-compatibility aliasing/shims introduced.

## Verification Layers

1. `cargo test --workspace` passes -> command output
2. `cargo clippy --workspace` produces no new warnings -> command output
3. Golden coverage docs include new S27 scenarios -> file inspection
4. IMPLEMENTATION-ORDER.md reflects S27 completion -> file inspection
5. Single-layer documentation ticket; additional verification layer mapping not applicable.

## What to Change

### 1. Final workspace verification

Run `cargo test --workspace` and `cargo clippy --workspace`. Confirm all existing and new tests pass with no new warnings.

### 2. Update `docs/golden-e2e-coverage.md`

Add entries for:
- S27-005: "Entity missing triggers investigation" — covers P15 (violated expectation), P7 (local observation), P12 (belief vs world state), P9 (investigation produces shareable aftermath)
- S27-006: "Violation triggers ShareBelief to co-located listener" — covers P1 (emergent reporting chain), P15 (violated expectation), P7 (local observation and physical information transfer)

### 3. Update golden scenario documentation

Update `docs/golden-e2e-scenarios.md` (or equivalent — `docs/generated/golden-scenario-map.md`, `docs/generated/golden-e2e-inventory.md`) with scenario descriptions for the two new golden tests.

### 4. Update `specs/IMPLEMENTATION-ORDER.md`

Add S27 completion entry in the "Completed Work" section and update the dependency graph to show `S27 ✅`.

### 5. Update spec status

Change `specs/S27-expectation-violation-goals.md` status from `PENDING` to `COMPLETED`.

## Files to Touch

- `docs/golden-e2e-coverage.md` (modify — add S27 coverage entries)
- `docs/golden-e2e-scenarios.md` or equivalent generated docs (modify — add scenario descriptions)
- `specs/IMPLEMENTATION-ORDER.md` (modify — mark S27 completed)
- `specs/S27-expectation-violation-goals.md` (modify — status to COMPLETED)

## Out of Scope

- Any code changes to `crates/` (all code is done in S27-001 through S27-006)
- Archiving the spec to `archive/specs/` (follows separate archival workflow in `docs/archival-workflow.md`)
- Creating new golden tests (done in S27-005, S27-006)
- Any implementation work

## Acceptance Criteria

### Tests That Must Pass

1. `cargo test --workspace` — all tests pass (existing + new S27 tests)
2. `cargo clippy --workspace` — no new warnings
3. Existing suite: no regressions across any crate

### Invariants

1. Golden coverage docs accurately reflect the new S27 scenarios and which principles they prove
2. IMPLEMENTATION-ORDER.md accurately reflects S27 completion status and dependency resolution
3. Spec status accurately reflects completion

## Test Plan

### New/Modified Tests

1. None — documentation-only ticket; verification is command-based and existing runtime coverage is named in Assumption Reassessment.

### Commands

1. `cargo test --workspace`
2. `cargo clippy --workspace`
