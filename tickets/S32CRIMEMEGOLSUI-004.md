# S32CRIMEMEGOLSUI-004: Update Golden Docs and Regenerate Artifacts for S32

**Status**: PENDING
**Priority**: MEDIUM
**Effort**: Small
**Engine Changes**: None — documentation only
**Deps**: S32CRIMEMEGOLSUI-001, S32CRIMEMEGOLSUI-002, S32CRIMEMEGOLSUI-003 (all three golden tests must be implemented first)

## Problem

After implementing the 3 new golden tests (Scenarios 41-43), the golden documentation and generated artifacts must be updated to reflect the new coverage. The hand-maintained `docs/golden-e2e-coverage.md` already lists the S32 scenarios in the pending backlog (line 25, line 29). After implementation, the backlog entries must be moved to the "implemented" section, and the generated scenario inventory/map must be regenerated from source annotations.

## Assumption Reassessment (2026-03-27)

1. **`docs/golden-e2e-coverage.md`** already contains S32 in the pending backlog at lines 25 and 29. After implementation, the entry at line 29 (`1. S32 crime emergence golden suites`) should be removed from the pending list, and the detailed entry at line 25 should be moved/updated to reflect "implemented" status.
2. **`scripts/golden_inventory.py`** exists and supports `--write --check-docs` flags. It reads structured metadata comments from golden test source files and regenerates `docs/generated/golden-scenario-map.md` and `docs/generated/golden-e2e-inventory.md`.
3. **`docs/generated/golden-scenario-map.md`** exists and will be regenerated by the script.
4. **Scenario metadata comments** must already be present in `golden_emergent.rs` from tickets 001-003. This ticket assumes those comments follow the format recognized by `golden_inventory.py`.
5. This is a documentation-only ticket — no Rust code changes.

## Architecture Check

1. Follows the established pattern from prior golden doc updates (S17, S18, S19, S16b-golden all had equivalent doc update tickets).
2. No backwards-compatibility aliasing/shims introduced.

## Verification Layers

1. Golden coverage docs list Scenarios 41-43 as implemented -> manual inspection of `docs/golden-e2e-coverage.md`
2. Generated scenario map includes S41-S43 metadata -> `python3 scripts/golden_inventory.py --write --check-docs` exits 0
6. Single-layer ticket — documentation artifacts only; no additional layer mapping applicable.

## What to Change

### 1. Update `docs/golden-e2e-coverage.md`

- Move S32 entry from "pending backlog" to the appropriate "implemented" section
- Update the description to reflect implemented status (remove "covers three E17-crime mechanics with zero golden coverage" phrasing, replace with "proves" language)
- Remove `1. S32 crime emergence golden suites` from the numbered pending list
- Update total test counts if the document tracks them

### 2. Regenerate golden artifacts

Run:
```bash
python3 scripts/golden_inventory.py --write --check-docs
```

This regenerates:
- `docs/generated/golden-scenario-map.md` — structured scenario metadata from source annotations
- `docs/generated/golden-e2e-inventory.md` — full test inventory
- `docs/generated/golden-coverage-matrix.md` — coverage matrix (if generated)

### 3. Verify generated output

Inspect the generated files to confirm Scenarios 41-43 appear with correct metadata (systems exercised, principles proven, places, GoalKinds).

## Files to Touch

- `docs/golden-e2e-coverage.md` (modify — move S32 from pending to implemented)
- `docs/generated/golden-scenario-map.md` (regenerated by script)
- `docs/generated/golden-e2e-inventory.md` (regenerated by script)
- `docs/generated/golden-coverage-matrix.md` (regenerated by script, if it exists)

## Out of Scope

- **No Rust code changes** — no modifications to any `.rs` file
- **No new golden tests** — those are tickets 001-003
- **No spec changes** — `specs/S32-crime-emergence-golden-suites.md` is not modified
- **IMPLEMENTATION-ORDER.md updates** — will be handled when S32 is fully closed out

## Acceptance Criteria

### Tests That Must Pass

1. `python3 scripts/golden_inventory.py --write --check-docs` exits with code 0
2. `cargo test --workspace` — no regressions (sanity check that no source was accidentally touched)

### Invariants

1. **No pending S32 entries**: `docs/golden-e2e-coverage.md` no longer lists S32 in the pending backlog
2. **Generated artifacts consistent**: `docs/generated/golden-scenario-map.md` includes metadata for Scenarios 41, 42, and 43
3. **Test count accurate**: if `docs/golden-e2e-coverage.md` tracks total golden test counts, the number reflects the 6 new tests (3 main + 3 replay companions)
4. **No Rust source changes**: `git diff --name-only` after this ticket shows only `.md` files

## Test Plan

### New/Modified Tests

1. None — documentation-only ticket; verification is command-based and existing runtime coverage is named in Assumption Reassessment.

### Commands

1. `python3 scripts/golden_inventory.py --write --check-docs`
2. `cargo test --workspace` (sanity check)
