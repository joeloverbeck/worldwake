# S139EPISENSUB-002: Extend EpistemicDispositionProfile with witness_recency_preference

**Status**: PENDING
**Priority**: HIGH
**Effort**: Small
**Engine Changes**: Yes — `worldwake-core` (`epistemic.rs`, `world.rs` test fixture, `save_load.rs` version bump)
**Deps**: specs/S139-epistemic-sensing-subgoals.md

## Problem

S139's emitter (ticket 004) and ranking (ticket 005) need a per-agent freshness-vs-distance weighting parameter (`witness_recency_preference`) that does not currently exist on any profile component. The reassessment confirmed that adding a parallel `EpistemicProfile` component would create dual-truth (FND-28) and semantic overlap (spec-drafting-rules 5f) with the existing universal `EpistemicDispositionProfile`. This ticket extends the existing component with one new field, regenerates the auto-generated profile doc, and bumps the save-format version because the serialized layout of a universal component changes.

## Assumption Reassessment (2026-05-13)

<!-- Apply all domain-specific precision rules from docs/precision-rules.md -->

1. `EpistemicDispositionProfile` is defined at `crates/worldwake-core/src/epistemic.rs:23` with three fields (`stale_evidence_barrier_threshold: Permille`, `witness_query_duration_ticks: NonZeroU32`, `ask_memory_retention_ticks: u32`) and a `Default` impl at line 35. It is registered as a universal component at `component_schema.rs:2119` and seeded in `world.rs::create_agent` at line 200. The belief-view accessor `epistemic_disposition_profile(actor)` exists at `belief_view.rs:548`, `1171`, `2020`. The struct derives `Serialize` and `Deserialize` (verified by the `epistemic_disposition: Some(EpistemicDispositionProfile::default())` deserialization sites in `scenario/lints.rs:364, 649, 665` and the RON scenarios at `scenarios/survival-ask-consult.ron`, `scenarios/cli-evaluation.ron`, `scenarios/final-integration.ron`).
2. `SAVE_FORMAT_VERSION` is `83` at `crates/worldwake-sim/src/save_load.rs:6`. Inline test assertions at lines 1232, 1243, 1250, 1453 reference this constant; the back-compat test at line 1730+ exercises `SAVE_FORMAT_VERSION - 1` decode failure. All five sites need the 83→84 bump.
3. Shared abstraction boundary under audit: `EpistemicDispositionProfile`'s serialized layout. Adding a `#[serde(default = "...")]`-annotated field is backward-compatible for bincode/serde decode (pre-bump bytes lack the field; the default helper supplies `pm(500)`). The version bump exists to make the format change auditable, not because pre-bump data fails to decode.
4. Explicit-construction sites that enumerate every field (no `..Default::default()` spread) — verified by direct grep for `stale_evidence_barrier_threshold:` workspace-wide:
   - `crates/worldwake-systems/src/ask_about_person_actions.rs:620`
   - `crates/worldwake-systems/src/epistemic_actions.rs:548`
   - `crates/worldwake-sim/src/action_semantics.rs:897`
   - `crates/worldwake-ai/src/planning_state.rs:4659`
   - `crates/worldwake-ai/src/goal_model.rs:7309`
   - `crates/worldwake-ai/src/search/tests.rs:921`
   - `crates/worldwake-core/src/world.rs:4970`
   - `crates/worldwake-core/src/epistemic.rs:93` (fixture mirroring `Default`)
   All 8 sites need `witness_recency_preference:` lines added.
5. RON scenarios that reference `epistemic_disposition` (`scenarios/survival-ask-consult.ron`, `scenarios/cli-evaluation.ron`, `scenarios/final-integration.ron`) do NOT need editing because the new field is `#[serde(default)]`-annotated.
6. `docs/profiles/all-profiles.md` is auto-generated by `scripts/profile_docs.py` (top-of-file docstring: "Parse Rust Profile structs and generate docs/profiles/all-profiles.md"). Regenerate via `python3 scripts/profile_docs.py --write` after the field lands.

## Architecture Check

1. Extending an existing universal component preserves single-source-of-truth for epistemic disposition (FND-28). Adding a sibling `EpistemicProfile` component would create dual truth — every agent would carry two epistemic-disposition profiles whose semantics overlap. The reassessment caught this; the spec was rewritten accordingly.
2. The `#[serde(default)]` annotation on the new field is a forward-compatibility mechanism *at the save-load boundary*, not a backward-compatibility shim in live authority paths. Live authority always reads the extended struct directly; the default helper only fires during save-file decode. FND-28 compliance preserved.

## Verification Layers

1. `EpistemicDispositionProfile` extension preserves serialized backward-compat for pre-bump bytes → save_load test asserting a pre-bump save deserializes with `witness_recency_preference = pm(500)` (the default helper value).
2. `SAVE_FORMAT_VERSION` bump 83 → 84 → save_load tests at lines 1232, 1243, 1250, 1453 updated; new test asserting the bump's effect on stream encoding.
3. Construction-site fan-out → workspace compiles (`cargo check --workspace`) with all 8 sites updated; no `..Default::default()` shortcuts available because `EpistemicDispositionProfile` doesn't currently use spread syntax at any construction site (verified by grep).
4. Single-layer ticket: this ticket touches stored state and serialization only; no candidate generation, no ranking, no goal-layer code. Additional-layer mapping is the regenerated `docs/profiles/all-profiles.md` artifact — verified by `python3 scripts/profile_docs.py` exit zero and the diff containing the new field.

## What to Change

### 1. Add `witness_recency_preference` field to `EpistemicDispositionProfile`

In `crates/worldwake-core/src/epistemic.rs:23-43`:

```rust
pub struct EpistemicDispositionProfile {
    pub stale_evidence_barrier_threshold: Permille,
    pub witness_query_duration_ticks: NonZeroU32,
    pub ask_memory_retention_ticks: u32,
    #[serde(default = "default_witness_recency_preference")]
    pub witness_recency_preference: Permille,
}

fn default_witness_recency_preference() -> Permille {
    Permille::new_unchecked(500)
}

impl Default for EpistemicDispositionProfile {
    fn default() -> Self {
        Self {
            stale_evidence_barrier_threshold: Permille::new_unchecked(400),
            witness_query_duration_ticks: NonZeroU32::new(2).unwrap(),
            ask_memory_retention_ticks: 12,
            witness_recency_preference: default_witness_recency_preference(),
        }
    }
}
```

### 2. Update 8 explicit-construction sites

Add `witness_recency_preference: <pm-value>` to each construction site listed in Assumption #4. For test fixtures, mirror the `Default` value (`pm(500)`). For the fixture at `epistemic.rs:93`, mirror `Default` exactly.

### 3. Bump SAVE_FORMAT_VERSION

In `crates/worldwake-sim/src/save_load.rs`:
- Line 6: `pub const SAVE_FORMAT_VERSION: u32 = 83;` → `84`.
- Lines 1232, 1243, 1250: update `assert_eq!(SAVE_FORMAT_VERSION, 83);` → `84`.
- Line 1453: same.

### 4. Regenerate profile doc

Run `python3 scripts/profile_docs.py --write` after the field lands. Commit the regenerated `docs/profiles/all-profiles.md` in the same ticket so the doc and code stay consistent.

### 5. Add forward-compat save-load test

In `crates/worldwake-sim/src/save_load.rs`'s `#[cfg(test)]` block, add a test asserting that a synthesized pre-bump (`SAVE_FORMAT_VERSION = 83`) byte stream containing the *old* `EpistemicDispositionProfile` layout decodes successfully under the new format and surfaces `witness_recency_preference = pm(500)` (the default helper's output). This documents the `#[serde(default)]` behavior at the save-load boundary.

## Files to Touch

- `crates/worldwake-core/src/epistemic.rs` (modify — field + Default + default helper)
- `crates/worldwake-core/src/world.rs` (modify — fixture at line 4970)
- `crates/worldwake-sim/src/save_load.rs` (modify — `SAVE_FORMAT_VERSION` bump + 4 assertions + 1 new test)
- `crates/worldwake-sim/src/action_semantics.rs` (modify — fixture at line 897)
- `crates/worldwake-ai/src/planning_state.rs` (modify — fixture at line 4659)
- `crates/worldwake-ai/src/goal_model.rs` (modify — fixture at line 7309)
- `crates/worldwake-ai/src/search/tests.rs` (modify — fixture at line 921)
- `crates/worldwake-systems/src/ask_about_person_actions.rs` (modify — construction at line 620)
- `crates/worldwake-systems/src/epistemic_actions.rs` (modify — construction at line 548)
- `docs/profiles/all-profiles.md` (regenerated by `scripts/profile_docs.py --write`)

## Out of Scope

- Consumers of `witness_recency_preference` (emitter ranking weighting in ticket 004, motive_score weighting in ticket 005). This ticket adds the field; downstream tickets read it.
- `EPISTEMIC_SENSING_POLICY` constant — ticket 003.
- `GoalKind::AskWitness` variant — ticket 001.
- Observer integration audit — ticket 006.

Merge note: Ticket 002 bumps SAVE_FORMAT_VERSION 83→84; no other ticket in S139EPISENSUB carries a save-format bump because the new field is `#[serde(default)]`-annotated and no other ticket changes a serialized authoritative type.

## Acceptance Criteria

### Tests That Must Pass

1. New focused unit test in `epistemic.rs`'s `#[cfg(test)]` block (at line 44) asserting `EpistemicDispositionProfile::default().witness_recency_preference == Permille::new_unchecked(500)`.
2. New save-load test in `save_load.rs`'s `#[cfg(test)]` block: pre-bump bytes (constructed manually with `SAVE_FORMAT_VERSION = 83` and the old 3-field layout) fail to decode (mismatched version) — confirms the version bump catches stale streams.
3. New save-load test: a new-format save (with `witness_recency_preference` serialized) round-trips through `save`/`load` and the field survives.
4. Existing `SAVE_FORMAT_VERSION` assertions at lines 1232, 1243, 1250, 1453 pass with the new value `84`.
5. Existing suite: `cargo test --workspace` passes.

### Invariants

1. `EpistemicDispositionProfile` `Default` impl stays consistent with the fixture at `epistemic.rs:93` — both authoritative defaults map to the same per-field values.
2. `SAVE_FORMAT_VERSION` is the single source of truth; no other version constant exists for this struct.
3. The new field has `#[serde(default)]` so pre-bump bytes encoded with `SAVE_FORMAT_VERSION = 83` decode into the new layout with the default value populated (verified by serde behavior; the version-bump assertion test confirms the version check is independent of the field-level default).
4. `docs/profiles/all-profiles.md` shows the new field; staleness is a verification failure (the doc-regen step is part of the ticket).

## Test Plan

### New/Modified Tests

1. `crates/worldwake-core/src/epistemic.rs` (extend `#[cfg(test)]` block at line 44) — new test asserting the default value of `witness_recency_preference`.
2. `crates/worldwake-sim/src/save_load.rs` (extend `#[cfg(test)]` block) — new test for the `#[serde(default)]` round-trip behavior under the bumped version.

### Commands

1. `cargo test -p worldwake-core -- epistemic::tests` — targeted run for the new default-value test.
2. `cargo test -p worldwake-sim -- save_load::tests` — targeted run for the save-load tests including the version-bump assertion.
3. `python3 scripts/profile_docs.py --check` — verify the regenerated doc matches the source struct (use `--write` to update, then run `--check` to confirm).
4. `cargo test --workspace` — confirm the 8 construction-site edits compile and existing tests pass.
5. `./scripts/verify.sh` — full pre-PR gate.
