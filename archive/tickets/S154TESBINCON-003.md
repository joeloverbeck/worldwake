# S154TESBINCON-003: Generated-doc regeneration + dual-glob retirement

**Status**: COMPLETED
**Priority**: HIGH
**Effort**: Medium
**Engine Changes**: None
**Deps**: archive/tickets/S154TESBINCON-002.md

## Problem

After ticket 002 moves the source files into the new `scenarios/` and `integration/` layout, two cleanup obligations remain:

1. `docs/generated/*` still reflects the pre-T2 source paths and per-file file-stem detail-page names
2. Ticket 001's `golden_inventory.py` still carries dual-layout glob and per-file invocation fallbacks; ticket 001's `test_golden_inventory.py` still carries pre-T2 cargo-output fixtures

Per FND-28, the transitional fallback must not survive the migration as fossilized dead code. Combining doc regeneration and dual-glob retirement in one ticket closes the migration cleanly without leaving a window where the script accepts two layouts but only one exists.

## Assumption Reassessment (2026-05-19)

1. The 4 generated docs are at `docs/generated/golden-e2e-inventory.md`, `docs/generated/golden-scenario-index.md`, `docs/generated/golden-scenario-details/*.md` (one per source file), and `docs/generated/golden-coverage-matrix.md`. Verified during reassessment. A fifth file (`docs/generated/scenario-coverage.md`) also exists in the directory but is generated by `cargo run -p worldwake-cli --bin scenario-coverage`, not by `golden_inventory.py` — explicitly out of scope for this ticket.
2. Ticket 001 introduced transitional dual-layout code in `scripts/golden_inventory.py` (the `tests/golden_*.rs` glob branch and the per-file `cargo test --test <stem>` invocation path) with in-code comments naming this ticket as the cleanup target. Ticket 001 also introduced pre-T2 fixtures in `scripts/test_golden_inventory.py` that this ticket removes.
3. Shared boundary under audit: post-T2 layout authority. After this ticket lands, the only layout the inventory script supports is `tests/scenarios/*.rs` + single-binary `cargo test -p worldwake-ai --test golden_ai -- --list`. The pre-T2 fallback path becomes dead code per FND-28 and must be removed, not fossilized.

## Architecture Check

1. Combining doc regeneration and dual-glob retirement in one ticket avoids a transient dead-code state: if regeneration landed first and retirement second, there would be a window where `golden_inventory.py` accepts both layouts but the codebase only contains one. Per the spec-to-tickets FND-28-driven combining rule, combining these eliminates a transient dead-branch state in the script.
2. The post-edit grep verification (matches only in docstrings/output strings, never in glob/invocation code paths) is the audit trail that proves the retirement was complete — not just that the doc regen succeeded.

## Verified Layers

1. Generated-doc byte-identity for post-T2 layout → `python3 scripts/golden_inventory.py --write --check-docs` exits 0
2. Dual-glob retirement → `grep -n 'golden_\*\.rs' scripts/golden_inventory.py scripts/test_golden_inventory.py` returns matches only in docstrings or generated-output strings, never in glob/invocation code paths
3. Test-fixture cleanup → `python3 scripts/test_golden_inventory.py` passes with only post-T2 fixtures (the pre-T2 fixtures from ticket 001 are removed; the post-T2 fixtures from ticket 001 remain)
4. Single-layer ticket: tooling cleanup with no engine effects. Action-trace / event-log / decision-trace verification surfaces are not applicable.

## Landed Changes

### 1. Regenerate `docs/generated/*`

`python3 scripts/golden_inventory.py --write --check-docs` now runs against the post-T2 layout and refreshed these files:

- `docs/generated/golden-e2e-inventory.md`
- `docs/generated/golden-scenario-index.md`
- `docs/generated/golden-scenario-details/*.md` (per-file detail pages — the underlying file names rename from `golden_<name>.md` to `<name>.md` per `_file_stem_to_detail_name()`)
- `docs/generated/golden-coverage-matrix.md`

`docs/generated/scenario-coverage.md` is generated by a separate binary and is explicitly out of scope.

### 2. Retire ticket 001's dual-layout glob

`scripts/golden_inventory.py` now keeps only the post-T2 layout:

- The `tests/golden_*.rs` glob branch was removed; discovery reads only `tests/scenarios/*.rs`
- The per-file `cargo test --test <stem> -- --list` invocation path was removed; compiled discovery uses only `cargo test -p worldwake-ai --test golden_ai -- --list`
- The runtime "does the new binary exist" check was removed; `golden_ai` is authoritative
- The temporary in-code comments and cleanup markers naming this ticket were removed

This replaces ticket 001's placeholder per the placeholder-replace pattern documented in 001's What to Change.

### 3. Retire ticket 001's pre-T2 test fixtures

`scripts/test_golden_inventory.py` now contains only post-T2 layout fixtures. The legacy source-file and per-binary cargo-output fixtures introduced for ticket 001 were removed; the retained fixture covers `Running tests/golden_ai.rs` plus module-path-bucketed test names.

## Landed Files

- `scripts/golden_inventory.py` (modify — retire dual-layout branches)
- `scripts/test_golden_inventory.py` (modify — retire pre-T2 fixtures)
- `docs/generated/golden-e2e-inventory.md` (regenerate)
- `docs/generated/golden-scenario-index.md` (regenerate)
- `docs/generated/golden-scenario-details/*.md` (regenerate; per-file detail-page rename from `golden_<name>.md` to `<name>.md`)
- `docs/generated/golden-coverage-matrix.md` (regenerate)

## Out of Scope

- Any source-file moves under `crates/worldwake-ai/tests/` — ticket 002's scope (already landed)
- Hand-authored doc edits to `docs/golden-e2e-testing.md`, `docs/debugging-traces.md`, `.claude/skills/*.md`, `CLAUDE.md` — ticket 005's scope
- `campaigns/golden-perf/harness.sh` rework — ticket 004's scope
- `docs/generated/scenario-coverage.md` — generated by `cargo run -p worldwake-cli --bin scenario-coverage`, unrelated to this script

## Acceptance Result

### Passed Proof

1. Passed `python3 scripts/golden_inventory.py --write --check-docs` against the post-T2 layout; it reported 54 files, 54 contributing files, 260 tests, and 196 scenario blocks.
2. Passed `python3 scripts/test_golden_inventory.py`; 8 tests passed with only post-T2 fixtures.
3. Passed `grep -n 'golden_\*\.rs' scripts/golden_inventory.py scripts/test_golden_inventory.py`; it returned zero matches.
4. Passed `./scripts/verify.sh`, whose live wrapper ran `cargo fmt --all -- --check`, `cargo test --workspace`, three repository removal checks, `cargo clippy --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo run -p worldwake-cli --bin scenario-coverage -- --check`.

### Invariants

1. The inventory script supports exactly one layout (`tests/scenarios/*.rs` + single-binary discovery), not two — no transitional fallback survives.
2. The 4 generated-doc surfaces reflect the post-T2 layout as the source of truth; no stale `golden_*.rs` source paths remain in their content.

## Test Plan Result

### Focused Tests

1. `scripts/test_golden_inventory.py` — removed pre-T2 fixtures and retained post-T2 fixture coverage for `golden_ai` module-path grouping.

### Commands Run

1. Passed `python3 scripts/test_golden_inventory.py`
2. Passed `python3 scripts/golden_inventory.py --write --check-docs`
3. Passed `grep -n 'golden_\*\.rs' scripts/golden_inventory.py scripts/test_golden_inventory.py` with zero matches
4. Passed `git diff --check`
5. Passed `./scripts/verify.sh`

## Outcome

Completed on 2026-05-19.

- Retired the transitional dual-layout inventory support from `scripts/golden_inventory.py`; source discovery now reads `crates/worldwake-ai/tests/scenarios/*.rs`, and compiled-test discovery always uses the consolidated `golden_ai` integration-test binary.
- Removed the pre-T2 unit-test fixtures from `scripts/test_golden_inventory.py`, leaving post-T2 fixture coverage for `golden_ai` module-path grouping and scenario source parsing.
- Regenerated the golden inventory, scenario index, scenario-detail pages, and coverage matrix so generated source references now name post-T2 scenario files such as `activation_decay.rs` rather than stale `golden_activation_decay.rs` paths.

## Deviations

- The drafted byte-stability command included `git diff --exit-code docs/generated/`, which is not a truthful post-regeneration witness before committing because this ticket intentionally changes generated docs. The landed proof is the generator's successful `--write --check-docs` run, followed by diff hygiene and the full `./scripts/verify.sh` wrapper.

## Verification Result

- Passed `python3 scripts/test_golden_inventory.py`
- Passed `python3 scripts/golden_inventory.py --write --check-docs`
- Passed `grep -n 'golden_\*\.rs' scripts/golden_inventory.py scripts/test_golden_inventory.py` with zero matches
- Passed `git diff --check`
- Passed `./scripts/verify.sh`
