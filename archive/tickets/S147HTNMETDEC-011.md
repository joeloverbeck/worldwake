# S147HTNMETDEC-011: Golden coverage for HTN selection and fallback

**Status**: COMPLETED
**Priority**: HIGH
**Effort**: Medium
**Engine Changes**: No — adds golden coverage under `crates/worldwake-ai/tests/`.
**Deps**: `archive/tickets/S147HTNMETDEC-008.md` (planner integration), `archive/tickets/S147HTNMETDEC-009.md` (trace + diagnostics), `archive/tickets/S147HTNMETDEC-010.md` (observer rendering), `archive/tickets/S147HTNMETDEC-012.md` (canonical recipe-input method preconditions)

## Problem

S147 needs golden coverage for the first stable HTN method seam. The component tests in tickets 001-010 prove registry, selector, trace, diagnostics, observer, and recipe-precondition behavior individually, but they did not add a `golden_*.rs` owner that appears in the generated golden inventory. This ticket adds that golden owner for the live method-selection and flat-fallback boundary.

## Assumption Reassessment (2026-05-17)

<!-- Apply all domain-specific precision rules from docs/precision-rules.md -->

1. `MethodRegistry`, `MethodSelector`, planner integration in `build_stages`, `PlanAttemptTrace.method_trace`, `Discrepancy::MethodFailure`, observer rendering all exist after tickets 001-010 land.
2. Existing golden test infrastructure lives at `crates/worldwake-ai/tests/golden_harness/` and `crates/worldwake-ai/tests/golden_*.rs`. The canonical golden inventory is regenerated by `python3 scripts/golden_inventory.py --write --check-docs` (per `tickets/README.md` line 8). New goldens added by this ticket must be regenerated into `docs/generated/golden-e2e-inventory.md` and `docs/generated/golden-scenario-index.md`.
3. Live `GoalKind` variants under test:
   - `ProduceCommodity { recipe_id }` — verified at `crates/worldwake-core/src/goal.rs`.
   - The method-disabled fallback case uses a `ProduceCommodity` goal with `disabled_methods` containing all three Produce methods, asserting flat-GOAP fallback executes.
4. Reassessment corrected the drafted six-scenario D10 shape. A focused autonomous production probe selected the flat strategic path even with a known remote resource source; the public selector selected `ProduceWithGather` only when the `GoalOffer` evidence places explicitly included the resource-source place. This ticket therefore owns the stable first golden seam: belief-backed selector proof plus agent-tick flat fallback trace. Full autonomous method-trace propagation for generated candidates was handed to the now-archived follow-up `archive/tickets/S147HTNMETDEC-013.md`.
5. Live `PlannerOpKind` variants exercised by the landed golden are `Craft` and the existing missing-input acquisition path. Bounty, investigation, escort, and method-failure action-lifecycle goldens are not completed by this ticket.
6. Shared boundary: the goldens prove the live S147 selection/fallback contract, not the entire future method-selection → subgoal-decomposition → execution → failure-attribution chain.
7. Existing focused/unit coverage in tickets 001-010 covers component behavior. Existing runtime trace/integration coverage exists via `PlanAttemptTrace` aggregation and the `agent_tick` test suite. Existing golden coverage for non-method scenarios (e.g., `golden_ai_decisions.rs`, `golden_plan_repair.rs`) must continue to pass — verifying that S147 introduces no regression on goals without methods (Non-Goal #4).

## Architecture Check

1. Golden scenarios are the authoritative regression guard per FND-31 (Validation and Falsification Are First-Class). The landed scenarios cover the stable ProduceWithGather selector path, method-denylist fallback, generated inventory participation, and deterministic replay for both observations.
2. The narrowed proof is cleaner than a forced full-action bounty/investigation fixture because the current live autonomous production candidate does not yet carry the selector evidence required to emit a method trace. The remaining autonomous bridge was tracked by the now-archived `archive/tickets/S147HTNMETDEC-013.md`.
3. No backwards-compatibility shims. All goldens are net-new.

## Verified Layers

1. Method choice is belief-backed and evidence-place gated → selector-level golden assertion using `PerAgentBeliefView`, `MethodSelector`, and a concrete `GoalOffer`.
2. Flat-GOAP fallback remains available when produce methods are disabled → agent-tick decision trace with `PlanAttemptTrace.method_trace == None` and a preserved missing-input strategic subgoal.
3. Determinism → replay companion tests for selector selection and disabled-method fallback observations.
4. Full autonomous method trace was deferred to the now-archived `archive/tickets/S147HTNMETDEC-013.md`; report-backed bounty investigation later landed in `archive/tickets/S147HTNMETDEC-014.md`; direct bounty, escort action lifecycle, and typed method-failure aftermath later landed through `archive/tickets/S147HTNMETDEC-015.md` and `archive/tickets/S147HTNMETDEC-016.md`.

## Landed Changes

### 1. New golden test file

Added `crates/worldwake-ai/tests/golden_htn_methods.rs` with two scenario blocks and four tests:

1. **`produce_with_gather_selector_uses_belief_view_evidence`** — a belief-backed `ProduceCommodity` `GoalOffer` whose evidence places include a known firewood source selects `MethodSchemaId(5)` (`ProduceWithGather`).
2. **`produce_with_gather_selector_replays_deterministically`** — repeats the selector observation and asserts equality.
3. **`disabled_produce_methods_fall_back_to_flat_strategic_search`** — disables all three Produce methods and verifies the agent-tick planning attempt has no method trace while preserving the flat missing-input strategic subgoal.
4. **`disabled_method_fallback_replays_deterministically`** — repeats the disabled-method fallback observation and asserts equality.

### 2. Regenerate golden inventory

Regenerated the golden inventory. The script updated `docs/generated/golden-e2e-inventory.md`, `docs/generated/golden-scenario-index.md`, `docs/generated/golden-coverage-matrix.md`, and added `docs/generated/golden-scenario-details/htn-methods.md`.

### 3. Scenario fixtures

No authored `scenarios/*.ron` fixture is required. The landed proof is a programmatic golden fixture because it needs to isolate the exact `GoalOffer` evidence-place boundary and the profile denylist without adding story-facing scenario content.

## Landed Files

- `crates/worldwake-ai/tests/golden_htn_methods.rs` (new)
- `docs/generated/golden-e2e-inventory.md` (regenerated by script)
- `docs/generated/golden-scenario-index.md` (regenerated by script)
- `docs/generated/golden-scenario-details/` (regenerated by script — per-file details)
- `docs/generated/golden-coverage-matrix.md` (regenerated by script)
- `archive/tickets/S147HTNMETDEC-013.md` (now-archived follow-up ticket for the autonomous production method-trace seam)
- `scripts/golden_inventory.py` (run only, no code change)

## Out of Scope

- Per-method observer formatting tweaks (ticket 010 owns observer surface).
- Adding new `PlannerOpKind` variants (would be a separate spec — first-ship uses only existing variants).
- Per-method TTL/backoff tuning in `failure_handling.rs` (`archive/tickets/S147HTNMETDEC-002.md`'s moderate defaults are sufficient; tuning is future work).
- Performance regression guards — S147 is not a performance-optimization spec; goldens cover correctness, not wall-clock metrics.
- Full autonomous method-trace propagation from generated production candidates — deferred to the now-archived `archive/tickets/S147HTNMETDEC-013.md`; report-backed bounty investigation later landed in `archive/tickets/S147HTNMETDEC-014.md`; remaining direct-bounty, escort/failure, and typed method-failure narratives later landed through `archive/tickets/S147HTNMETDEC-015.md` and `archive/tickets/S147HTNMETDEC-016.md`.

## Acceptance Result

### Tests Passed

1. All 4 new tests in `golden_htn_methods.rs` pass.
2. Existing `worldwake-ai` tests continue to pass — regression guard for "no behavioral regression on goals without methods" (Non-Goal #4).
3. `python3 scripts/golden_inventory.py --write --check-docs` passes the `--check-docs` validation (generated docs are consistent with the golden test surface).
4. Existing suite: `cargo test -p worldwake-ai` passes (full ai-crate test suite).
5. `cargo clippy --workspace --all-targets -- -D warnings` clean.
6. `./scripts/verify.sh` remains the final branch/pre-push family gate and is not claimed by this ticket iteration.

### Invariants

1. Determinism — the selector observation and fallback observation are byte-stable under repeated fixed-seed runs.
2. Selector locality — `ProduceWithGather` is selected only from the actor's belief view plus the `GoalOffer` evidence places, not from omniscient world state.
3. Flat-GOAP fallback equivalence — when methods are disabled, the planning attempt records no method trace and still exposes the ordinary missing-input strategic subgoal.
4. No new `PlannerOpKind` variants — this ticket adds tests and generated docs only.

## Test Plan Result

### Added Tests

1. `crates/worldwake-ai/tests/golden_htn_methods.rs` — new — 2 scenario blocks / 4 tests for the live S147 selector and fallback seam.

### Verification Commands

1. `cargo test -p worldwake-ai --test golden_htn_methods`
2. `python3 scripts/golden_inventory.py --write --check-docs`
3. `cargo test -p worldwake-ai` (full ai test suite — regression guard)
4. `cargo clippy --workspace --all-targets -- -D warnings`

## Outcome

Completed on 2026-05-17.

- Added the first S147 HTN golden owner, `crates/worldwake-ai/tests/golden_htn_methods.rs`, with two scenario metadata blocks and four tests.
- Regenerated golden inventory/index/detail/matrix docs for the added `golden_htn_methods.rs` coverage.
- Created the now-archived `archive/tickets/S147HTNMETDEC-013.md` for the remaining autonomous method-trace propagation and original full-D10 narratives; after `archive/tickets/S147HTNMETDEC-014.md`, the remaining direct-bounty, escort/failure, and typed method-failure narratives later landed through `archive/tickets/S147HTNMETDEC-015.md` and `archive/tickets/S147HTNMETDEC-016.md`.

## Deviations

- The original six-scenario D10 draft was narrowed. The live autonomous production probe fell back to flat planning even with known firewood source beliefs, while the direct selector proof selected `ProduceWithGather` when the `GoalOffer` carried the source place as evidence. This ticket therefore lands the stable selector/fallback golden seam and hands the candidate-evidence bridge plus full autonomous D10 coverage to the now-archived `archive/tickets/S147HTNMETDEC-013.md`; after `archive/tickets/S147HTNMETDEC-014.md`, remaining direct-bounty, escort/failure, and typed method-failure narratives later landed through `archive/tickets/S147HTNMETDEC-015.md` and `archive/tickets/S147HTNMETDEC-016.md`.
- No authored `scenarios/*.ron` fixture landed; the proof uses a programmatic golden fixture to isolate the exact selector evidence and disabled-method profile boundary.
- `./scripts/verify.sh` was not run for this ticket iteration. The completed proof used the focused golden binary, the golden inventory generator, the full `worldwake-ai` test suite, and CI-matching all-target workspace clippy. The harness still owns `./scripts/verify.sh` before final branch push.

## Verification Result

- Passed `cargo test -p worldwake-ai --test golden_htn_methods`.
- Passed `python3 scripts/golden_inventory.py --write --check-docs`.
- Passed `cargo test -p worldwake-ai`.
- Passed `cargo clippy --workspace --all-targets -- -D warnings`.
