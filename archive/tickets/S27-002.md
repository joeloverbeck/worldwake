# S27-002: Add InvestigateMissing GoalKind variant and planner support

**Status**: ✅ COMPLETED
**Priority**: HIGH
**Effort**: Medium
**Engine Changes**: Yes — new GoalKind variant, GoalKindTag, PlannerOpKind, goal policy, ranking, feasibility dispatch
**Deps**: S27-001 (needs `ViolationDispositionProfile` for ranking motive extraction)

## Problem

No `GoalKind` variant exists to represent "investigate why an expected entity is missing at a location." Without this variant and its planner/ranking/policy integration, the AI pipeline cannot select, plan, or execute investigation goals even after violation detection emits candidates.

## Assumption Reassessment (2026-03-25)

1. `GoalKind` in `crates/worldwake-core/src/goal.rs` currently has 17 variants. `GoalKey` extraction uses a match arm per variant. Adding variant 18 (`InvestigateMissing { place: EntityId }`) follows the established pattern. Confirmed by file read.
2. `GoalKindTag` in `crates/worldwake-ai/src/goal_model.rs` mirrors `GoalKind` with 17 variants. `GoalKindPlannerExt` trait has methods: `goal_kind_tag()`, `relevant_op_kinds()`, `relevant_observed_commodities()`, `is_satisfied()`, `goal_relevant_places()`, `prerequisite_places()`, `build_payload_override()`, `apply_planner_step()`, `is_progress_barrier()`, `matches_binding()`. Each must be implemented for the new variant. Confirmed by file read.
3. `PlannerOpKind` in `crates/worldwake-ai/src/planner_ops.rs` has op-kind constants and `build_semantics_table()`. The new `Investigate` op needs semantics: `may_appear_mid_plan: false`, `is_materialization_barrier: false`, `transition_kind: GoalModelFallback`. Confirmed by file read.
4. `goal_family_policy()` in `crates/worldwake-ai/src/goal_policy.rs` dispatches on `GoalKindTag`. Confirmed pattern.
5. `rank_candidates()` in `crates/worldwake-ai/src/ranking.rs` uses `ranked_priority_class()` and `ranked_motive_score()` which dispatch on `GoalKindTag`. Confirmed pattern.
6. `feasibility_hint()` in `crates/worldwake-ai/src/feasibility.rs` dispatches on `GoalKindTag` via `goal_specific_feasibility()`. Confirmed pattern.
7. This is a planner-driven ticket. The live `GoalKind` under test is `InvestigateMissing`. The operator surface is `PlannerOpKind::Investigate` with terminal check at investigation place. Affordance surface requires the `investigate` action def (S27-004) but this ticket only wires the planner — affordance/action registration is out of scope.

## Architecture Check

1. Follows the exact pattern of existing goal variants (e.g., `ShareBelief`, `ClaimOffice`): GoalKind variant + GoalKindTag + PlannerOpKind + policy + ranking + feasibility. No novel patterns.
2. No backwards-compatibility aliasing/shims introduced.

## Verification Layers

1. `GoalKind::InvestigateMissing` extracts correct `GoalKey` with `place` field -> focused unit test
2. `GoalKindTag::InvestigateMissing` dispatches correctly in `goal_kind_tag()` -> compile-time exhaustive match
3. `PlannerOpKind::Investigate` semantics are correct (`may_appear_mid_plan: false`, etc.) -> focused unit test on `build_semantics_table()`
4. Goal policy suppression at `High` stress -> focused unit test
5. Ranking assigns `GoalPriorityClass::Low` and motive from profile -> focused unit test
6. Feasibility returns Likely when co-located, Uncertain when travel needed, Unlikely when blocked -> focused unit test
7. `matches_binding()` for `InvestigateMissing` — terminal check on place -> focused unit test
8. `GoalKindPlannerExt` methods compile and are exhaustive -> compile-time enforcement via match arms

## What to Change

### 1. Add `GoalKind::InvestigateMissing { place: EntityId }` in `crates/worldwake-core/src/goal.rs`

New variant. Update `GoalKey` extraction match arm: `GoalKind::InvestigateMissing { place } => GoalKey { kind: GoalKind::InvestigateMissing { place }, commodity: None, entity: None, place: Some(place) }`.

### 2. Add `GoalKindTag::InvestigateMissing` in `crates/worldwake-ai/src/goal_model.rs`

New variant. Implement all `GoalKindPlannerExt` methods:
- `goal_kind_tag()`: return `GoalKindTag::InvestigateMissing`
- `relevant_op_kinds()`: return `&GOALS_INVESTIGATE` (new constant: `&[PlannerOpKind::Investigate]`)
- `relevant_observed_commodities()`: return empty (no commodity involved)
- `is_satisfied()`: satisfied when investigate action can start at the place (terminal)
- `goal_relevant_places()`: return `[place]`
- `prerequisite_places()`: return `[place]` (agent must be at investigation place)
- `build_payload_override()`: return `None` (no special payload)
- `apply_planner_step()`: handle `Investigate` op — marks goal progressed
- `is_progress_barrier()`: travel to investigation place is a progress barrier
- `matches_binding()`: terminal check — affordance must target the investigation place

### 3. Add `PlannerOpKind::Investigate` in `crates/worldwake-ai/src/planner_ops.rs`

New variant. Add `GOALS_INVESTIGATE` constant. Add semantics in `build_semantics_table()`: `may_appear_mid_plan: false`, `is_materialization_barrier: false`, `transition_kind: GoalModelFallback`.

### 4. Add goal policy in `crates/worldwake-ai/src/goal_policy.rs`

`GoalKindTag::InvestigateMissing => GoalFamilyPolicy { suppression: WhenStressedAtOrAbove(GoalPriorityClass::High), penalty_interrupt: Never, free_interrupt: FreeInterruptRole::Normal }`.

### 5. Add `violation_disposition_profile` to belief view traits

Ranking needs to read `ViolationDispositionProfile` for motive scoring. Add:
- `fn violation_disposition_profile(&self, agent: EntityId) -> Option<ViolationDispositionProfile>` with default `None` to `GoalBeliefView` in `crates/worldwake-sim/src/belief_view.rs`.
- Same method to `RuntimeBeliefView` in the same file with default `None`.
- Implement on `OmniscientBeliefView` in `crates/worldwake-sim/src/omniscient_belief_view.rs` reading from `World` component tables.
- Implement on `PerAgentBeliefView` in `crates/worldwake-sim/src/per_agent_belief_view.rs` reading from `World` component tables (profile is not belief-filtered — it's the agent's own disposition).
- Add to the `impl_goal_belief_view_for_runtime!` macro bridge in `belief_view.rs`.
- Update `MockView` in `crates/worldwake-ai/src/feasibility.rs` tests.

### 6. Add ranking in `crates/worldwake-ai/src/ranking.rs`

`GoalKindTag::InvestigateMissing`:
- Priority class: `GoalPriorityClass::Low`
- Motive: base from `ViolationDispositionProfile.investigation_motive_weight.as_u32()`, ownership bonus from `ownership_motive_bonus.as_u32()` if agent owns the missing entity (evidence entity lookup).

### 7. Add feasibility dispatch in `crates/worldwake-ai/src/feasibility.rs`

`GoalKindTag::InvestigateMissing`:
- `Likely` if agent's effective_place matches investigation place
- `Uncertain` if agent is not at investigation place
- `Unlikely` if `BlockedIntentMemory` has unexpired entry for the GoalKey

## Files to Touch

- `crates/worldwake-core/src/goal.rs` (modify — new variant + GoalKey extraction)
- `crates/worldwake-ai/src/goal_model.rs` (modify — new GoalKindTag + GoalKindPlannerExt impl)
- `crates/worldwake-ai/src/planner_ops.rs` (modify — new PlannerOpKind + semantics)
- `crates/worldwake-ai/src/goal_policy.rs` (modify — new policy entry)
- `crates/worldwake-sim/src/belief_view.rs` (modify — add `violation_disposition_profile` to GoalBeliefView + RuntimeBeliefView + macro bridge)
- `crates/worldwake-sim/src/omniscient_belief_view.rs` (modify — implement `violation_disposition_profile`)
- `crates/worldwake-sim/src/per_agent_belief_view.rs` (modify — implement `violation_disposition_profile`)
- `crates/worldwake-ai/src/ranking.rs` (modify — new ranking entry)
- `crates/worldwake-ai/src/feasibility.rs` (modify — new feasibility dispatch)

## Out of Scope

- Core types (`ViolationKind`, `ViolationMemory`, `ViolationDispositionProfile`) — S27-001
- Candidate generation (`emit_expectation_violation_candidates()`) — S27-003
- Investigate action definition/handler — S27-004
- Golden tests — S27-005, S27-006
- IntentionFrame creation for investigation goals (happens in existing decision runtime when any goal is selected — no new code needed here)
- Changes to `worldwake-sim` or `worldwake-systems`

## Acceptance Criteria

### Tests That Must Pass

1. `GoalKind::InvestigateMissing { place }` produces correct `GoalKey` with `place: Some(place)`, `commodity: None`, `entity: None`
2. `GoalKindTag::InvestigateMissing` is returned by `goal_kind_tag()` for the new variant
3. `PlannerOpKind::Investigate` semantics: `may_appear_mid_plan == false`, `is_materialization_barrier == false`
4. Goal policy for `InvestigateMissing`: suppression at `High`, penalty interrupt `Never`, free interrupt `Normal`
5. Ranking assigns `GoalPriorityClass::Low` for `InvestigateMissing`
6. Feasibility returns `Likely` when agent is at investigation place
7. Existing suite: `cargo build --workspace` (exhaustive match arms compile)
8. Existing suite: `cargo clippy --workspace`
9. Existing suite: `cargo test -p worldwake-ai` (no regressions in existing goal handling)

### Invariants

1. All `GoalKindTag` match arms remain exhaustive (compile-time enforcement)
2. `InvestigateMissing` motive score uses only `ViolationDispositionProfile` fields — no hardcoded constants
3. Feasibility dispatch covers all three hint levels (Likely/Uncertain/Unlikely)
4. No new `DirtySet` domain needed (existing `BELIEF_ENTITIES` and `POSITION` domains cover violation detection inputs)

## Test Plan

### New/Modified Tests

1. `crates/worldwake-ai/src/goal_model.rs` (inline tests) — GoalKindPlannerExt methods for InvestigateMissing
2. `crates/worldwake-ai/src/goal_policy.rs` (inline tests) — policy values for InvestigateMissing
3. `crates/worldwake-ai/src/ranking.rs` (inline tests) — priority class and motive score for InvestigateMissing
4. `crates/worldwake-ai/src/feasibility.rs` (inline tests) — feasibility hint dispatch for InvestigateMissing
5. `crates/worldwake-core/src/goal.rs` (inline tests) — GoalKey extraction for InvestigateMissing

### Commands

1. `cargo test -p worldwake-core`
2. `cargo test -p worldwake-ai`
3. `cargo clippy --workspace`
4. `cargo build --workspace`

## Outcome

- **Completion date**: 2026-03-25
- **What changed**:
  - `GoalKind::InvestigateMissing { place }` variant + GoalKey extraction in `worldwake-core/src/goal.rs`
  - `GoalKindTag::InvestigateMissing` + full `GoalKindPlannerExt` impl (10 methods) in `worldwake-ai/src/goal_model.rs`
  - `PlannerOpKind::Investigate` + semantics in `worldwake-ai/src/planner_ops.rs`
  - Goal policy in `worldwake-ai/src/goal_policy.rs`
  - `violation_disposition_profile()` method on `GoalBeliefView`, `RuntimeBeliefView`, macro bridge, and `PerAgentBeliefView`
  - Ranking (priority Low, motive from ViolationDispositionProfile) in `worldwake-ai/src/ranking.rs`
  - Feasibility dispatch in `worldwake-ai/src/feasibility.rs`
  - Exhaustive match arms updated across 6 additional files
- **Deviations from original ticket**:
  - Added step 5 (belief view trait method) not in original ticket — `violation_disposition_profile()` needed on `GoalBeliefView`/`RuntimeBeliefView` for ranking to read the profile. Ticket corrected before implementation.
  - Used `u32::from(profile.investigation_motive_weight.value())` instead of ticket's `as_u32()` — `Permille` has `value() -> u16`, not `as_u32()`.
- **Verification**: `cargo build --workspace` ✅, `cargo clippy --workspace` ✅, `cargo test --workspace` ✅ (all tests pass)
