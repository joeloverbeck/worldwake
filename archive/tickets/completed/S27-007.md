# S27-007: Workspace verification, documentation alignment, and archival

**Status**: ✅ COMPLETED
**Priority**: LOW
**Effort**: Small
**Engine Changes**: None
**Deps**: S27-005, S27-006, S27-008, S27-009; spec `archive/specs/S27-expectation-violation-goals.md`

## Problem

S27 implementation is already live, but the final documentation/archive pass was stale. The active ticket still described S27 as a docs-only closeout for a place-scoped investigation design, while the codebase had already moved to the cleaner incident-identity architecture and shipped the relevant focused and golden coverage. The remaining work is to verify the workspace, align the docs/spec inventory to the shipped state, and archive the ticket/spec with an accurate outcome.

## Assumption Reassessment (2026-03-25)

1. The original assumption that S27 was still pending implementation was incorrect. Live code already contains `ViolationMemory`, `ViolationId`, `ViolationDispositionProfile`, `SocialObservationKind::WitnessedAbsence`, and `GoalKind::InvestigateViolation { violation_id, place }` in [crates/worldwake-core/src/violation.rs](/home/joeloverbeck/projects/worldwake/crates/worldwake-core/src/violation.rs) and [crates/worldwake-core/src/goal.rs](/home/joeloverbeck/projects/worldwake/crates/worldwake-core/src/goal.rs).
2. The live goal family under test is not the older spec draft `InvestigateMissing { place }`. The shipped architecture uses exact incident identity through `GoalKind::InvestigateViolation { violation_id, place }`, payload-aware planning in [crates/worldwake-ai/src/goal_model.rs](/home/joeloverbeck/projects/worldwake/crates/worldwake-ai/src/goal_model.rs), and authoritative exact-id investigate execution in [crates/worldwake-systems/src/investigate_actions.rs](/home/joeloverbeck/projects/worldwake/crates/worldwake-systems/src/investigate_actions.rs).
3. The relevant focused/runtime and golden coverage already exists. Current examples include `candidate_generation::tests::violation_entity_missing_emits_investigate_candidate`, `candidate_generation::tests::same_place_distinct_violations_emit_distinct_investigate_goals`, `candidate_generation::tests::unresolved_recorded_violation_reemits_candidate_without_new_pending_record`, focused investigate-action coverage in [crates/worldwake-systems/src/investigate_actions.rs](/home/joeloverbeck/projects/worldwake/crates/worldwake-systems/src/investigate_actions.rs), and goldens `golden_entity_missing_triggers_investigation`, `golden_supply_depletion_enables_share_belief`, and `golden_same_place_concurrent_violations_stay_distinct` in [crates/worldwake-ai/tests/golden_emergent.rs](/home/joeloverbeck/projects/worldwake/crates/worldwake-ai/tests/golden_emergent.rs).
4. The canonical golden documentation split is different from the ticket’s original wording. [docs/golden-e2e-coverage.md](/home/joeloverbeck/projects/worldwake/docs/golden-e2e-coverage.md) is the interpretive dashboard, while [docs/generated/golden-scenario-map.md](/home/joeloverbeck/projects/worldwake/docs/generated/golden-scenario-map.md), [docs/generated/golden-e2e-inventory.md](/home/joeloverbeck/projects/worldwake/docs/generated/golden-e2e-inventory.md), and [docs/generated/golden-coverage-matrix.md](/home/joeloverbeck/projects/worldwake/docs/generated/golden-coverage-matrix.md) are the canonical generated artifacts. There is no active `docs/golden-e2e-scenarios.md` file.
5. The documentation state is partly already aligned. [docs/golden-e2e-coverage.md](/home/joeloverbeck/projects/worldwake/docs/golden-e2e-coverage.md) already records S27-005 and S27-006 as removed backlog items because they shipped as live scenarios, and the generated scenario map already includes Scenario 36 and Scenario 37 in [crates/worldwake-ai/tests/golden_emergent.rs](/home/joeloverbeck/projects/worldwake/crates/worldwake-ai/tests/golden_emergent.rs).
6. The actual remaining discrepancy was the planning/archive layer: [specs/IMPLEMENTATION-ORDER.md](/home/joeloverbeck/projects/worldwake/specs/IMPLEMENTATION-ORDER.md) still listed S27 as an active Wave 3 item and still kept `S27-expectation-violation-goals.md` in the active spec inventory, while the spec now archived at [archive/specs/S27-expectation-violation-goals.md](/home/joeloverbeck/projects/worldwake/archive/specs/S27-expectation-violation-goals.md) was still marked `**Status**: PENDING` before this closeout.
7. The better long-term architecture is the one already shipped, not the older ticket/spec narrative. Exact `violation_id` identity plus explicit unresolved/resolved lifecycle is cleaner, more robust, and more extensible than place-scoped investigate goals or handler-side inference. This closeout ticket must not re-open the older architecture in documentation.
8. Corrected scope: this ticket is a verification/documentation/archive closeout only. It should not propose new engine changes, but it must update the ticket/spec text to match the shipped S27 architecture and archive both records accordingly.

## Architecture Check

1. The right closeout preserves the shipped incident-identity architecture instead of documenting the older place-scoped draft as if it were final. `InvestigateViolation { violation_id, place }` with explicit resolution state is the cleaner design because it keeps concrete incident identity consistent across detection, planning, execution, aftermath, and future extensibility.
2. This is better than trying to retro-fit the docs to the original place-only story. The place-only shape was superseded because it collapsed distinct incidents and forced lossy inference at execution time. The current architecture stands the test of time better.
3. No backwards-compatibility aliases or shims are introduced.

## Verification Layers

1. Workspace regression boundary for shipped S27 behavior -> `cargo test --workspace`
2. Static/lint boundary for shipped S27 codepaths -> `cargo clippy --workspace`
3. Canonical golden-doc alignment -> `python3 scripts/golden_inventory.py --write --check-docs`
4. Spec/roadmap closeout accuracy -> file inspection of `specs/IMPLEMENTATION-ORDER.md` plus archived S27 spec/ticket
5. This is a documentation/archive ticket; additional action-trace or event-log layer mapping is not applicable because production behavior is already covered by existing focused and golden tests named above.

## What to Change

### 1. Run final verification for the shipped S27 architecture

Run the full workspace test and lint boundary, plus the golden-doc generation check, to confirm the already-implemented S27 stack and its docs still pass cleanly.

### 2. Update implementation-order and active-spec inventory

Mark S27 completed in [specs/IMPLEMENTATION-ORDER.md](/home/joeloverbeck/projects/worldwake/specs/IMPLEMENTATION-ORDER.md), update the Wave 3 dependency line to `S27 ✅`, and remove the active-spec-inventory row so the file matches the archival state.

### 3. Mark the S27 spec completed and archive it with an accurate outcome

Update the S27 spec now archived at [archive/specs/S27-expectation-violation-goals.md](/home/joeloverbeck/projects/worldwake/archive/specs/S27-expectation-violation-goals.md) to `COMPLETED` and add an `Outcome` section that records the important deviations from the draft design: exact `violation_id` goal identity, explicit unresolved/resolved lifecycle, and shipped focused/golden coverage.

### 4. Archive this ticket with the final closeout record

Archive this ticket under `archive/tickets/completed/` with an `Outcome` section describing what was actually needed: verification, implementation-order cleanup, spec archival, and confirmation that the generated golden docs were already aligned.

## Files to Touch

- `tickets/S27-007.md` (modify, then archive)
- `specs/IMPLEMENTATION-ORDER.md` (modify)
- `specs/S27-expectation-violation-goals.md` (modify, then archive)

## Out of Scope

- new production changes in `crates/`
- reworking the already-shipped `InvestigateViolation` architecture back toward place-scoped investigate goals
- adding new S27 tests beyond existing focused and golden coverage
- hand-editing generated golden docs unless regeneration proves them stale

## Acceptance Criteria

### Tests That Must Pass

1. `cargo test --workspace`
2. `cargo clippy --workspace`
3. `python3 scripts/golden_inventory.py --write --check-docs`

### Invariants

1. Active planning/docs no longer describe S27 as pending work once the ticket is archived.
2. The archived S27 record reflects the shipped incident-identity architecture rather than the older place-scoped draft.
3. Canonical golden docs remain aligned with the live source-declared S27 scenarios.

## Test Plan

### New/Modified Tests

1. None — documentation/archive closeout ticket; verification relies on the existing focused and golden S27 coverage named in Assumption Reassessment.

### Commands

1. `python3 scripts/golden_inventory.py --write --check-docs`
2. `cargo test --workspace`
3. `cargo clippy --workspace`

## Outcome

Completion date: 2026-03-25

What actually changed:

1. Reassessed the ticket against the live repo and corrected the stale assumption that S27 was still a place-scoped docs-only closeout.
2. Updated implementation-order and active-spec inventory so S27 is recorded as completed rather than still active.
3. Marked the S27 spec completed, documented the final architecture/outcome, and archived it.
4. Archived this ticket with the final verification and closeout record.

Deviations from original plan:

1. No production or golden-doc content changes were needed for S27 behavior itself because the implementation and generated docs were already live and aligned.
2. The important closeout work was accuracy of the planning/archive record, not adding new scenario prose files or re-documenting a non-existent `docs/golden-e2e-scenarios.md`.
3. The final architecture worth preserving is the shipped exact-incident design (`InvestigateViolation` plus explicit violation lifecycle), not the older place-only draft described in parts of the original ticket/spec.

Verification results:

1. `python3 scripts/golden_inventory.py --write --check-docs`
2. `cargo test --workspace`
3. `cargo clippy --workspace`
