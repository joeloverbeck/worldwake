# S27-008: Thread stable violation identity through goals and investigate execution

**Status**: ✅ COMPLETED
**Priority**: HIGH
**Effort**: Large
**Engine Changes**: Yes — `worldwake-core` violation/goal identity, `worldwake-ai` investigation goal generation and binding, `worldwake-sim` payload/binding support, `worldwake-systems` investigate affordance and handler
**Deps**: S27-001, S27-002, S27-003, S27-004; spec `specs/S27-expectation-violation-goals.md`

## Problem

The live S27 pipeline now detects and records concrete expectation violations, but it still collapses distinct violations into a place-scoped investigation goal before execution. That means two different anomalies at the same place currently share one goal identity and the authoritative `investigate` handler resolves “the latest matching violation at this place” instead of the exact violation the planner selected.

That is workable for the first delivery of `investigate`, but it is not the ideal architecture. It weakens concrete-state traceability, makes same-place concurrent anomalies ambiguous, and creates the wrong foundation for future evidence, report, accusation, or repeated-investigation logic. The clean fix is to give each recorded violation stable identity and carry that identity all the way from violation detection to authoritative action commit.

## Assumption Reassessment (2026-03-25)

1. `ViolationMemory` and violation-driven candidate generation already exist live. In `crates/worldwake-core/src/violation.rs`, `RecordedViolation` currently stores `{ kind, observed_tick, expires_tick }`, and `ViolationMemory::record()` deduplicates by `ViolationKind` only. There is no stable `ViolationId` yet, but the ticket must not describe violation memory as missing.
2. The live goal under test is still `GoalKind::InvestigateMissing { place }` in `crates/worldwake-core/src/goal.rs`. `GoalKey::from()` therefore collapses investigation identity to place only.
3. `emit_expectation_violation_candidates()` and `emit_violation_goal()` already exist in `crates/worldwake-ai/src/candidate_generation.rs`. They emit investigation goals from concrete `ViolationKind` observations, but the emitted goal keeps only `place`, so distinct same-place incidents collapse before search.
4. `PlannerOpKind::Investigate` is already the live terminal operator in `crates/worldwake-ai/src/planner_ops.rs`, and `GoalKind::matches_binding()` in `crates/worldwake-ai/src/goal_model.rs` currently checks investigate identity through authoritative targets only: `authoritative_targets.contains(place)`.
5. The live investigate action still uses `ActionPayload::None` in `crates/worldwake-systems/src/investigate_actions.rs`, and `start_investigate()` reconstructs the chosen incident by scanning `ViolationMemory` for the newest unexpired matching violation at the actor's place. That handler-side recovery is the precise ambiguity boundary.
6. Mismatch + correction: the ticket's proposed "new binding surface" is too broad. The repo already has the right payload architecture: affordance payload enumeration, payload override validation, authoritative payload validation, and payload-sensitive revalidation through `requested_affordance_matches()` in `crates/worldwake-sim/src/affordance_query.rs` and `build_payload_override()` in `crates/worldwake-ai/src/goal_model.rs`. Investigate should reuse that path rather than inventing a generic new binding mechanism.
7. The current focused coverage is broader than the original ticket claimed. Live tests already cover `ViolationMemory`, violation candidate generation, investigate registration/start/commit/abort behavior, and place-only investigate binding. What is still missing is focused proof that same-place sibling violations stay distinct through goal identity, search payload selection, authoritative start, and commit.
8. `specs/S27-expectation-violation-goals.md` still describes the goal family as `InvestigateMissing { place }`. That spec remains useful background, but its goal contract is now stale relative to the cleaner incident-identity design this ticket should implement.
9. FOUNDATIONS still points in one direction: Principles 3, 4, 15, 16, and 25 all favor making the incident itself explicit and threading that identity through the system rather than recovering it from a lossy place cache.
10. Corrected scope: this ticket is a cross-layer identity-threading fix spanning `worldwake-core` violation storage and goal identity, `worldwake-ai` candidate/search payload synthesis, `worldwake-sim` action payload definitions, and `worldwake-systems` investigate affordance/start/commit validation.

## Architecture Check

1. The clean architectural move is to treat a recorded violation as a first-class incident with stable identity. That keeps investigation grounded in a concrete record instead of a place-only summary and aligns with FOUNDATIONS Principles 3, 4, 15, 16, and 25.
2. The goal family should be renamed to reflect what the system actually investigates. A violation-based goal such as `GoalKind::InvestigateViolation { violation_id, place }` is cleaner than `InvestigateMissing { place }`, which is already semantically narrower than live behavior because `SupplyDepleted` maps into the same family.
3. The place field should remain only as derived locality/search guidance. The authoritative identity is the `ViolationId`.
4. The cleanest execution path is to carry `violation_id` via the existing action payload architecture:
   - enumerate one investigate affordance per active investigable violation at the actor's place
   - validate the payload through the existing payload override hooks
   - let AI search/planning carry the chosen payload through normal payload synthesis and revalidation
   - validate the same id authoritatively at start and commit
5. This is better than a new generic "payload-aware binding surface" because the repo already has a proven, reusable payload identity contract for office, social, combat, and production actions. Reusing that contract keeps the architecture smaller and more extensible.
6. No compatibility alias should preserve the old place-only investigate path. Once stable violation identity exists, the place-only goal and handler-side "latest matching violation" fallback should be removed.

## Verification Layers

1. Stable violation identity allocation, refresh, expiry, and rediscovery semantics -> focused `worldwake-core` unit tests on `ViolationMemory`
2. Distinct same-place violations produce distinct AI goal identities and blocker identities -> focused `worldwake-ai` unit tests on candidate generation and goal keying
3. Search selection and revalidation distinguish same-place investigate affordances by payload identity, not target identity alone -> focused `worldwake-ai` unit tests on payload override synthesis and plan revalidation
4. Investigate affordance enumeration, payload validation, and authoritative start use the exact violation id, not "latest at place" fallback -> focused `worldwake-systems` runtime/action tests
5. Commit aftermath is attached to the exact investigated violation identity and does not silently consume or overwrite a sibling same-place violation -> focused authoritative world-state tests
6. End-to-end same-place concurrency behavior -> golden coverage in follow-up ticket `S27-009`

## What to Change

### 1. Give recorded violations stable identity in `worldwake-core`

Add a stable `ViolationId` value type and store it on `RecordedViolation`.

Update `ViolationMemory` so it owns incident identity explicitly. The component should:

- allocate a new id when a newly discovered violation kind is first recorded
- refresh the existing record for the same still-active violation kind instead of creating a second id
- allocate a new id if the earlier record expired and the violation is rediscovered later
- expose exact-id lookup helpers needed by AI and authoritative investigate execution

If a monotonic allocator is kept inside `ViolationMemory`, it must remain deterministic and agent-local.

### 2. Replace place-only investigation goals with violation-aware goals

Rename the live goal family to reflect what the system actually investigates. A concrete target shape such as `GoalKind::InvestigateViolation { violation_id, place }` is preferred.

Update:

- `crates/worldwake-core/src/goal.rs`
- `GoalKey` extraction so distinct violation ids do not collapse into one key
- `crates/worldwake-ai/src/candidate_generation.rs`
- `crates/worldwake-ai/src/goal_model.rs`
- `crates/worldwake-ai/src/planner_ops.rs`
- `crates/worldwake-ai/src/goal_policy.rs`
- `crates/worldwake-ai/src/ranking.rs`
- `crates/worldwake-ai/src/feasibility.rs`
- `crates/worldwake-ai/src/failure_handling.rs`

The goal family must remain belief-driven and locality-preserving. The agent still discovers the violation locally; this ticket only stops the system from erasing the discovered incident identity before action selection.

### 3. Carry exact violation identity through the investigate action contract

Add a dedicated investigate payload in `worldwake-sim` for the non-entity incident identity, then update the investigate affordance/selection/start/commit path to require that payload.

The live action should:

- continue using actor-place targeting for the physical location
- enumerate one affordance payload per active investigable violation at that place
- carry `violation_id` through payload, not through handler-side inference
- validate at start and commit that the referenced record is still active and still matches the bound place
- reject stale, expired, wrong-place, or non-investigable investigate payloads cleanly

`ActionState::Investigate` should store the chosen `violation_id` in addition to any resolved subject/source facts needed at commit.

### 4. Reuse the existing AI payload path for exact investigate selection

Do not add a brand new generic investigate-binding API unless implementation proves the current payload path is insufficient. The preferred design is:

- `GoalKind::InvestigateViolation { violation_id, place }` carries exact identity at the goal layer
- `build_payload_override()` synthesizes the matching investigate payload for search candidates
- search/revalidation continue using payload-sensitive affordance matching through the existing `requested_affordance_matches()` contract

This keeps target binding responsible for locality (`place`) and payload identity responsible for the non-target incident id.

### 5. Strengthen focused tests for same-place ambiguity

Add focused tests that prove:

- two active violations at the same place produce two distinct investigation goals
- search/revalidation for one same-place violation does not silently accept the sibling payload
- starting investigate for one violation does not silently bind the sibling violation
- completing or blocking one violation does not suppress the other purely because the place matches
- re-observing the same still-active violation refreshes its record without allocating a second id

## Files to Touch

- `crates/worldwake-core/src/violation.rs` (modify)
- `crates/worldwake-core/src/goal.rs` (modify)
- `crates/worldwake-ai/src/candidate_generation.rs` (modify)
- `crates/worldwake-ai/src/goal_model.rs` (modify)
- `crates/worldwake-ai/src/planner_ops.rs` (modify)
- `crates/worldwake-ai/src/goal_policy.rs` (modify)
- `crates/worldwake-ai/src/ranking.rs` (modify)
- `crates/worldwake-ai/src/feasibility.rs` (modify)
- `crates/worldwake-ai/src/failure_handling.rs` (modify)
- `crates/worldwake-ai/src/plan_revalidation.rs` (modify if tests or payload-sensitive coverage need updates)
- `crates/worldwake-sim/src/action_payload.rs` (modify)
- `crates/worldwake-sim/src/action_state.rs` (modify)
- `crates/worldwake-systems/src/investigate_actions.rs` (modify)

## Out of Scope

- new violation kinds beyond the current `EntityMissing`, `SupplyDepleted`, and `EntityDead`
- evidence-chain systems, accusation systems, warrants, or crime-scene world artifacts
- social-reporting behavior beyond whatever existing `ShareBelief` already does
- backward-compatibility wrappers that keep place-only investigate goals alive beside the new identity-bearing path
- documentation/roadmap archival updates beyond what is required to keep the ticket/spec text accurate

## Acceptance Criteria

### Tests That Must Pass

1. `ViolationMemory` stores a stable `ViolationId` on each active recorded violation
2. Re-recording the same unexpired violation refreshes the existing record instead of creating a second active id
3. Rediscovering the same violation after expiry allocates a new id
4. Distinct same-place violations produce distinct investigation goals in candidate generation
5. Search/revalidation rejects an investigate affordance or requested step with the wrong `violation_id` even if the actor-place target matches
6. The authoritative investigate action starts and commits against the exact referenced violation id, not “latest matching violation at this place”
7. Completing or failing one same-place violation does not suppress a sibling violation solely because they share a place
8. Existing suite: `cargo test -p worldwake-core`
9. Existing suite: `cargo test -p worldwake-ai --lib`
10. Existing suite: `cargo test -p worldwake-systems investigate_action -- --nocapture`
11. Existing suite: `cargo clippy --workspace`

### Invariants

1. Investigation binds to a concrete recorded violation, not a lossy place-only summary
2. Place remains a locality constraint, not the authoritative incident identity
3. No handler-only fallback path infers incident identity after selection
4. The design remains belief-driven and locality-preserving
5. No compatibility alias keeps the old place-only investigation path alive

## Test Plan

### New/Modified Tests

1. `crates/worldwake-core/src/violation.rs` — add stable-id allocation, refresh, expiry, and rediscovery coverage for `ViolationMemory`
2. `crates/worldwake-core/src/goal.rs` — update goal-key identity coverage so distinct violation ids do not collapse
3. `crates/worldwake-ai/src/candidate_generation.rs` — add focused same-place multi-violation candidate coverage
4. `crates/worldwake-ai/src/goal_model.rs` and/or `crates/worldwake-ai/src/plan_revalidation.rs` — add focused payload synthesis/revalidation coverage proving same-place investigate candidates remain distinct by `violation_id`
5. `crates/worldwake-systems/src/investigate_actions.rs` — add affordance/runtime tests proving wrong-id and stale-id investigate requests fail cleanly and sibling same-place violations remain distinct

### Commands

1. `cargo test -p worldwake-core`
2. `cargo test -p worldwake-ai --lib`
3. `cargo test -p worldwake-systems investigate_action -- --nocapture`
4. `cargo test -p worldwake-ai golden_entity_missing_discovery_does_not_teleport_belief -- --exact`
5. `cargo clippy --workspace`

## Outcome

- Completed: 2026-03-25
- What actually changed:
  - Added stable `ViolationId` allocation and exact-id lookup/refresh semantics to `ViolationMemory`.
  - Replaced the place-only investigate goal with `GoalKind::InvestigateViolation { violation_id, place }`.
  - Added `ActionPayload::Investigate` and stored `violation_id` in `ActionState::Investigate`.
  - Updated AI candidate generation, payload synthesis, and investigate matching so same-place sibling violations remain distinct through planning and revalidation.
  - Updated investigate affordance enumeration, payload validation, start, and commit so execution binds to the exact recorded violation instead of recovering “latest matching violation at place”.
- Deviations from original plan:
  - No new generic binding system was introduced; the existing payload override and affordance revalidation architecture was sufficient and remained the cleaner long-term design.
  - `crates/worldwake-ai/src/failure_handling.rs` and `crates/worldwake-ai/src/plan_revalidation.rs` did not need production changes once payload-aware investigate binding was implemented through the existing goal-model and affordance matching surfaces.
  - Added `RuntimeBeliefView::active_violation_records()` in `worldwake-sim` so runtime payload-aware investigate validation could read active agent-local violation records cleanly.
- Verification results:
  - `cargo test -p worldwake-core`
  - `cargo test -p worldwake-ai --lib`
  - `cargo test -p worldwake-systems investigate_action -- --nocapture`
  - `cargo test -p worldwake-ai golden_entity_missing_discovery_does_not_teleport_belief -- --exact`
  - `cargo clippy --workspace`
