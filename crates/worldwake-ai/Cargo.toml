[package]
name = "worldwake-ai"
version = "0.1.0"
edition = "2021"

[dependencies]
bincode = "1"
serde = { version = "1", features = ["derive"] }
worldwake-core = { path = "../worldwake-core" }
worldwake-sim = { path = "../worldwake-sim" }
worldwake-systems = { path = "../worldwake-systems" }

[features]
bench-profiling = ["worldwake-sim/bench-profiling"]
soak = []

[dev-dependencies]
blake3 = "1"

[lints]
workspace = true
