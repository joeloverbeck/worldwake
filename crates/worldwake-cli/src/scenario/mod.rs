//! Scenario system — RON-based world initialization.
//!
//! `types` defines the deserialization schema (`ScenarioDef` and sub-structs).
//! Spawning logic (`spawn_scenario()`) lives in a separate module (E21CLIHUMCON-002).

pub mod types;
