[package]
name = "worldwake-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
bincode = "1"

[dev-dependencies]
rand_chacha = "0.3"

[lints]
workspace = true
